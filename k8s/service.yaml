apiVersion: v1
kind: Service
metadata:
  name: eventhorizon-backend
  namespace: eventhorizon
spec:
  selector:
    app: eventhorizon-backend
  ports:
    - name: http
      port: 80
      targetPort: 8000
