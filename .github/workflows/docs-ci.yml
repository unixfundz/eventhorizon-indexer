name: Docs CI

on:
  push:
    paths:
      - "eventhorizon-indexer/docs/**"
      - "eventhorizon-indexer/README.md"
      - ".github/workflows/docs-ci.yml"
  pull_request:
    paths:
      - "eventhorizon-indexer/docs/**"
      - "eventhorizon-indexer/README.md"

jobs:
  markdown-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: DavidAnson/markdownlint-cli2-action@v18
        with:
          globs: |
            eventhorizon-indexer/**/*.md

  link-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check markdown links
        uses: gaurav-nelson/github-action-markdown-link-check@v1
        with:
          use-quiet-mode: "yes"
          config-file: eventhorizon-indexer/.markdown-link-check.json
          folder-path: eventhorizon-indexer
